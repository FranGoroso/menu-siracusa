// Importamos solo los íconos específicos que necesitamos para reducir el tamaño del bundle
import { Coffee } from 'lucide-react';
import { ChevronDown } from 'lucide-react';
import { ChevronUp } from 'lucide-react';
import { Star } from 'lucide-react';
import { Heart } from 'lucide-react';

// Re-exportar los íconos específicos que usamos en nuestra aplicación
export { Coffee, ChevronDown, ChevronUp, Star, Heart };
